# Optional: endpoint that receives each completed assessment as JSON (POST).
# Leave unset and submissions are archived in the browser's localStorage instead.
VITE_LEAD_ENDPOINT=

# Optional: where the "Book a Free 15-min Session" button sends people.
VITE_BOOKING_URL=
